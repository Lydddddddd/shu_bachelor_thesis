# 毕设18周周记

## 第1周
本周主要完成毕业设计方向的确定与相关文献调研，重点阅读了多模态大模型、ViT、CLIP、MoE 和推理加速相关论文。结合研究背景，初步明确了论文的核心问题是 VL-MoE 推理中的视觉前端开销和专家路由开销。

## 第2周
本周继续梳理 VL-MoE 的整体结构，重点分析视觉编码器、LLM 主干和 MoE 专家层在推理链路中的位置与作用。与此同时，搭建了基础实验环境，整理后续 profiling 所需的模型、数据和运行依赖。

## 第3周
本周开始对基线模型进行性能分析，重点统计首字延迟 TTFT、视觉编码耗时和专家层访问特征。通过初步 profiling 发现，Visual Encoder 在推理前段占据了较高比例的计算时间，是需要优先处理的瓶颈之一。

## 第4周
本周围绕视觉侧瓶颈进一步分析图像 Token 数量、Patch 序列长度与自注意力复杂度之间的关系。通过对不同输入样本的统计，确认高分辨率图像会显著增加视觉前端负担，因此需要设计能够减少冗余 Token 的压缩方法。

## 第5周
本周开始设计第三章的方法框架，提出以文本语义为引导的视觉 Token 压缩思路。方法上确定使用文本查询与视觉 Token 做相关性打分，并根据语义分布的集中程度动态决定保留比例。

## 第6周
本周完成文本特征聚合与相似度计算模块的设计，明确了全局文本特征需要先投影到视觉特征空间，再与每个视觉 Token 计算余弦相似度。与此同时，整理了 Top-K 选择和保留索引输出的接口形式，确保后续能够接入视觉编码器。

## 第7周
本周实现基于熵的自适应压缩率计算逻辑，并完成了视觉 Token 保留数量的动态控制。通过小规模样例验证，确认当语义分布集中时可以提高压缩率，而当语义分布分散时会自动保留更多 Token。

## 第8周
本周开始设计视觉编码器的双阶段执行流程，将 Visual Encoder 划分为 Stage 1 和 Stage 2，并在中间插入 SAT-ToMe 压缩模块。这样可以在保留主要语义信息的同时，减少后续视觉层需要处理的 Token 数量。

## 第9周
本周完成异步流水的工程实现，使用独立 CUDA stream 将视觉侧计算与文本侧准备并行执行。与此同时，补齐了变长序列相关的实现细节，包括 rotary position embedding、`cu_seqlens` 和 `max_seqlen` 的更新。

## 第10周
本周开展视觉侧实验，选取 MMBench 与 MME 子集作为效果评估数据集，对随机裁剪、Static ToMe、SAT-ToMe 和 SAT-ToMe + Pipeline 进行对比。实验结果表明，在相同平均保留比例下，语义引导方法能更好地保持任务分数与输出一致率。

## 第11周
本周继续整理第三章实验结果，重点分析 SAT-ToMe 对 TTFT 和 Visual Encoder 可见耗时的影响。结合 profiling 数据，可以看出视觉前端的压缩与异步执行能够显著降低前段等待时间。

## 第12周
本周开始转向第四章，重点研究 MoE 推理中的路由规律和专家访问模式。通过分析层间路由 trace，确认后续层的专家选择与第 0 层 Router 输出存在可学习的相关性，因此可以尝试构建全局专家预测器。

## 第13周
本周设计全局路由预测器的输入特征和网络结构，包括 layer-0 router 概率、Top-k 专家 multi-hot、token 位置、input id 哈希嵌入、模态标识和 router 统计量。模型主体采用轻量级 MLP backbone，并为未来各层设置独立输出头预测专家 logits。

## 第14周
本周完成预测器训练与离线评估流程，使用 BCEWithLogitsLoss 对多标签专家预测进行监督。验证结果显示，预测器能够学到较稳定的跨层路由规律，mean recall 达到较好的水平。

## 第15周
本周进一步设计预测驱动的受限预取与缓存调度策略，定义了候选专家打分、层距衰减、置信度阈值、每层上限和全局预算等约束。调度逻辑上加入 protected set，避免为了预取未来专家而影响当前层必须访问的专家。

## 第16周
本周实现真实 CPU-GPU expert offload 微基准，构建 demand、predictor 和 oracle 三种策略进行对比。通过统计同步加载次数、缓存命中次数和总耗时，验证了预测式预取在减少等待时间方面的实际收益。

## 第17周
本周系统整理第五章实验结果，并将第三章和第四章的关键结论统一到论文叙事中。与此同时，补充了附录中的关键实现代码说明，使方法流程、训练逻辑和调度逻辑能够前后对应。

## 第18周
本周完成全文修改与定稿，包括术语统一、图表位置调整、参考文献补充和答辩材料整理。最终形成了“视觉 Token 压缩 + 异步流水 + 专家预测 + 受限预取调度”的完整研究闭环，并完成论文与答辩 PPT 的最终版本。
