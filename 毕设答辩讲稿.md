# 《多模态混合专家模型的推理优化》10 分钟答辩讲稿

使用方式：按页照读即可。章节页快过，重点页稍微放慢。总时长约 9 分 50 秒到 10 分 20 秒。

## 第 1 页 标题页 20 秒

各位老师好，我是李耀东，我的毕设题目是《多模态混合专家模型的推理优化》。

本文关注的是视觉语言混合专家模型，也就是 VL-MoE，在推理阶段的效率问题。我的工作主要从两个方向展开：一是降低视觉编码器前端开销，二是利用专家路由的可预测性减少 offload 场景下的等待时间。

## 第 2 页 目录 15 秒

今天的汇报分为四部分：研究背景与问题定义、负载分析与优化目标、方法设计与系统实现，以及实验结果与效果分析。

## 第 3 页 第一部分 5 秒

首先介绍研究背景与问题定义。

## 第 4 页 研究背景 30 秒

多模态大模型近年来发展很快，图像和文本联合建模已经成为主流。同时，MoE 结构通过稀疏激活专家，可以在扩大模型规模的同时控制单 token 计算开销。

但 VL-MoE 的推理链路也更复杂：图像会产生大量视觉 token，MoE 又需要动态选择专家。因此系统同时面临视觉前端计算重、专家调度复杂和资源利用率不足的问题。

## 第 5 页 问题定义 30 秒

本文关注三个具体瓶颈。

第一，视觉 token 冗余。图像中很多背景区域也会被编码成 token，带来无效计算。

第二，视觉编码器开销高。所有视觉 token 都参与 attention，计算量随 token 数增加而明显增长，并直接影响 TTFT。

第三，MoE 路由动态。token 需要动态选择 expert，给通信、缓存和 offload 调度带来困难。

## 第 6 页 研究现状 20 秒

相关工作主要分两类。

视觉侧有 ToMe、PuMer、IVTP 和 ZSPAPRUNE，重点是 token 合并、筛选或压缩。MoE 推理侧有 Klotski、DistServe、DALI 和 ExpertFlow，重点是调度、解耦和 offload 优化。

本文也是沿着这两条线展开：视觉侧减少冗余，专家侧提前调度。

## 第 7 页 第二部分 5 秒

接下来介绍 profiling 分析和优化目标。

## 第 8 页 关键发现一 30 秒

第一个发现是：多模态请求里图像 token 占比非常高。

我在 MMBench 和 MME 上统计输入 token 构成，发现 image token 占比超过 95%，text token 占比很低。

这说明 VL-MoE 虽然最终生成文本，但推理前端的大部分负载其实来自图像。因此视觉 token 冗余是一个必须优先处理的问题。

## 第 9 页 关键发现二 35 秒

第二个发现是：Visual Encoder 是 TTFT 的主要前端瓶颈。

我在 4 张 A6000 GPU 上对 Qwen3-VL-30B-A3B-Instruct 做端到端 profiling，将过程拆成 Visual Encoder、LLM Prefill、MoE 计算和 All-to-All 通信。

结果显示，Visual Encoder 占 TTFT 约 20.6%。同时 MoE 阶段 All-to-All 通信频繁，说明专家侧也存在调度和通信优化空间。

## 第 10 页 关键发现三 30 秒

第三个发现是：MoE 路由具有结构性和可预测性。

我在 Router 处插入 hook，记录每一层 token 选择的 Top-K experts，并统计专家分布和层间关系。

结果发现，单层专家整体比较均衡，没有特别极端的热点；但不同层之间存在路径依赖，所以可以尝试用前面层的路由信息预测未来专家。

## 第 11 页 路由可预测性 20 秒

这一页是对上一页的直观总结。

token 在网络中存在层间聚集效应，这意味着未来专家并不是完全随机的。只要预测得足够准，就可以提前预取专家，把一部分等待时间从关键路径上移走。

## 第 12 页 优化思路 25 秒

基于 profiling，本文形成三条优化思路。

第一，image token 占比高，所以做语义感知 token 压缩。第二，Encoder 是 TTFT 瓶颈，所以做 Encoder Pipeline。第三，专家路由有跨层相关性，所以做专家预测和预取。

## 第 13 页 第三部分 5 秒

下面进入方法设计与系统实现。

## 第 14 页 方法一：语义感知 Token 压缩 40 秒

第一个方法是语义感知的自适应视觉 token 压缩。

核心思想是：不是随机删除视觉 token，而是用文本问题判断哪些视觉区域更重要。

具体来说，先提取全局文本特征，并投影到视觉特征空间；然后计算每个视觉 token 和文本特征的余弦相似度，得到语义相关分数。

接着对分数做 softmax 并计算熵。熵低说明问题关注集中，可以压缩得更强；熵高说明需要保留更多图像信息。最后根据自适应的 K 值保留 Top-K 视觉 token。

## 第 15 页 方法二：异步流水 35 秒

第二个方法是异步流水线。

Token 压缩解决的是“少算”，但如果流程仍然串行，TTFT 仍然会高。因此我把视觉编码器拆成 Stage 1 和 Stage 2，并使用独立的 `vision_stream` 执行视觉侧计算。

主流处理文本 embedding 和后续推理准备，视觉流处理图像预处理、Stage 1、SAT-ToMe 和 Stage 2。只在必须交换信息时同步，从而让视觉计算和文本侧准备尽量重叠。

## 第 16 页 方法三：全局专家预测器 40 秒

第三个方法是全局专家路由预测器。

它不替代原始 Router，而是利用第 0 层已经产生的路由信号，提前预测后续所有 MoE 层可能访问的专家集合。

输入包括 layer-0 router 概率、Top-k 专家、token 位置、input id、模态标识和路由统计量。结构上采用共享 MLP backbone 加多层输出头，直接输出后续层专家 logits。

因为同一层可能访问多个专家，所以这是多标签预测任务，训练时使用 BCEWithLogits 来优化未来专家集合的 recall。

## 第 17 页 方法四：受限预取与缓存调度 35 秒

第四个方法是预测驱动的受限预取和缓存调度。

预测器会给出未来高概率专家，但不能全部搬到 GPU，因为 cache 容量有限，预测也可能出错。

所以调度器会先形成候选专家，再根据置信度、层距衰减和 margin 计算专家价值；同时用每层上限和全局预算控制预取规模。

当前层真实需要的专家会被保护，不能被预取挤出。如果预测失败，就回退到同步加载，保证正确性。

## 第 18 页 第四部分 5 秒

下面介绍实验结果。

## 第 19 页 实验一：效果保持 40 秒

首先看视觉压缩是否影响模型效果。

这里用 MMBench 和 MME 子集评估，并使用归一化任务分数，也就是优化后分数除以 baseline 分数。

可以看到，随机裁剪下降明显，说明视觉 token 不能无差别删除。Static ToMe 有改善，但仍有损失。

SAT-ToMe 在 63.7% 保留比例下，MMBench 达到 0.997，MME 达到 0.995，输出一致率为 99.1%。这说明文本语义引导可以在减少 token 的同时基本保持模型效果。

## 第 20 页 实验二：TTFT 加速 35 秒

第二个实验是视觉编码器耗时。

Baseline 中 Visual Encoder 耗时 8572 ms，占 TTFT 的 20.6%。加入 SAT-ToMe 后，Encoder 降到 1088 ms；再叠加 Pipeline 后，可见耗时进一步降到 800 ms。

整体 TTFT 获得约 1.63 倍加速，Encoder 自身加速达到 10.72 倍，说明视觉侧优化显著压缩了前端瓶颈。

## 第 21 页 实验三：预测器结果 30 秒

接下来是全局专家预测器。

预测器在验证集上的 best mean recall 达到 0.7219，early、mid、late 三个视野分别是 0.7158、0.7453 和 0.7042。

这说明预测器不是盲猜，而是确实从 layer-0 router 概率、Top-k、位置、input id 和模态信息中学到了跨层路由规律。

## 第 22 页 实验四：真实 offload 微基准 40 秒

最后是真实 CPU-GPU expert offload 微基准。

demand 表示真实访问发生后再同步加载，predictor 表示使用本文方法提前预取，oracle 是使用真实未来路由的上界。

在 cap96 下，predictor 将耗时从 4.283 秒降到 1.432 秒，获得 2.99 倍加速；在 cap160 下，获得 2.66 倍加速。

虽然 predictor 的传输量有时更高，但它把同步等待变成了受控预取，所以总耗时明显下降。

## 第 23 页 主要工作与创新点 35 秒

本文主要工作有四点。

第一，完成 VL-MoE 推理负载分析，定位视觉侧和专家侧双重瓶颈。

第二，提出语义感知视觉压缩与异步流水，降低 TTFT。

第三，构建全局专家预测器和受限预取调度机制。

第四，在真实 CPU-GPU expert 搬运微基准中验证方法收益，获得 2.99 倍和 2.66 倍加速。

## 第 24 页 不足与展望 25 秒

本文还有两个不足。

第一，视觉侧和专家侧目前主要是分别验证，后续需要接入完整端到端 serving 框架，在真实在线负载下验证整体收益。

第二，当前路由数据主要覆盖单一模型和有限数据集，后续需要扩展到更多任务、长上下文和不同 VL-MoE 架构，提升泛化能力。

## 第 25 页 致谢 5 秒

以上就是我的汇报内容，恳请各位老师批评指正，谢谢大家。

